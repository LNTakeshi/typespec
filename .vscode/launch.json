{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "スクリプトの実行: start",
            "request": "launch",
            "command": "tsp compile . --debug",
            "cwd": "${workspaceFolder}\\"
        }
    ]
}